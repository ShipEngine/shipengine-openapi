title: address_validation_result
type: object
properties:
  status:
    allOf:
      - $ref: 'enums.yaml#/address_validation_status'
  original_address:
    allOf:
      - $ref: address.yaml
  matched_address:
    allOf:
      - $ref: address.yaml
  messages:
    type: array
    items:
      title: response_message
      type: object
      properties:
        code:
          allOf:
            - $ref: 'enums.yaml#/code'
        message:
          type: string
        type:
          allOf:
            - $ref: 'enums.yaml#/type'
        detail_code:
          allOf:
            - $ref: 'enums.yaml#/detail_code'
