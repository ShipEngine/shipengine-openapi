title: batch
type: object
properties:
  label_layout:
    type: string
  label_format:
    allOf:
      - $ref: 'enums.yaml#/label_format'
  batch_id:
    allOf:
      - $ref: 'primitives.yaml#/se_id'
    description: A string that uniquely identifies the batch id
  external_batch_id:
    allOf:
      - $ref: 'primitives.yaml#/se_id'
    description: A string that uniquely identifies the external batch id
  batch_notes:
    type: string
  created_at:
    type: string
    format: date-time
  processed_at:
    type: string
    format: date-time
  errors:
    type: integer
    format: int32
  warnings:
    type: integer
    format: int32
  completed:
    type: integer
    format: int32
  forms:
    type: integer
    format: int32
  count:
    type: integer
    format: int32
  batch_shipments_url:
    allOf:
      - $ref: link.yaml
  batch_labels_url:
    allOf:
      - $ref: link.yaml
  batch_errors_url:
    allOf:
      - $ref: link.yaml
  label_download:
    allOf:
      - $ref: link.yaml
  form_download:
    allOf:
      - $ref: link.yaml
  status:
    allOf:
      - $ref: 'enums.yaml#/batch_status'
