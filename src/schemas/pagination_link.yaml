title: pagination_link
type: object
properties:
  first:
    $ref: link.yaml
  last:
    $ref: link.yaml
  prev:
    $ref: link.yaml
  next:
    $ref: link.yaml
  href:
    type: string
  type:
    type: string
