title: bulk_rate_response_body
type: array
items:
  title: bulk_rate
  type: object
  additionalProperties: false
  properties:
    rate_request_id:
      allOf:
        - $ref: '../primitives.yaml#/se_id'
      description: A string that uniquely identifies the rate request
    shipment_id:
      allOf:
        - $ref: '../primitives.yaml#/se_id'
      description: A string that uniquely identifies the shipment
    created_at:
      type: string
      format: date-time
    status:
      $ref: '../enums.yaml#/rate_response_status'
    errors:
      type: array
      items:
        $ref: ../provider_error.yaml
