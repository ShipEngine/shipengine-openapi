title: shipment_package
type: object
additionalProperties: false
properties:
  package_code:
    type: string
  weight:
    additionalProperties: false
    allOf:
      - $ref: weight.yaml
  dimensions:
    additionalProperties: false
    allOf:
      - $ref: dimensions.yaml
  insured_value:
    additionalProperties: false
    allOf:
      - $ref: money.yaml
  label_messages:
    additionalProperties: false
    allOf:
      - $ref: label_messages.yaml
