title: shipment_package
type: object
additionalProperties: false
properties:
  package_code:
    type: string
  weight:
    $ref: weight.yaml
  dimensions:
    $ref: dimensions.yaml
  insured_value:
    $ref: monetary_value.yaml
  label_messages:
    $ref: label_messages.yaml
