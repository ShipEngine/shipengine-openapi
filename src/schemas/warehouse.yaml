title: warehouse
type: object
properties:
  warehouse_id:
    allOf:
      - $ref: 'primitives.yaml#/se_id'
    description: A string that uniquely identifies the warehouse id
  name:
    type: string
  created_at:
    type: string
    format: date-time
  origin_address:
    $ref: address.yaml
  return_address:
    $ref: address.yaml
