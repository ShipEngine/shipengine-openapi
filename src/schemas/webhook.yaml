title: webhook
type: object
additionalProperties: false
properties:
  webhook_id:
    allOf:
      - $ref: 'primitives.yaml#/se_id'
    description: A string that uniquely identifies the webhook id
  url:
    type: string
  event:
    type: string
